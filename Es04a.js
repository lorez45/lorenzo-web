// stampa numeri da 100 a 0 con passo 20

for (let i = 100; i >= 0; i -= 20) { // crea un ciclo: i parte da 100, continua finché i è >= 0, e diminuisce di 20 ad ogni iterazione
    console.log(i); // stampa nella console il valore attuale della variabile i
}
